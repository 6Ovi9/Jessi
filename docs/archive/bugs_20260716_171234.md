# Comprehensive Codebase Audit Report
**Date:** 2026-07-16 17:12:34

This document aggregates the findings from the multi-agent codebase audit across all architectural layers. Despite some subagents timing out at the very end, we successfully captured their complete JSON payloads and summary tables.

## 1. Backend & Infrastructure
*Note: An exhaustive security and configuration audit found 29 issues previously (including critical credential exposure and RLS misconfigurations). Fixes are currently being verified by the reviewer agents.*

## 2. Flutter Models & Screens (20 Issues)
**Critical/High:**
* `settings_screen.dart`: `_startCalibration` is dead code — never called (HIGH)
* `settings_screen.dart`: Callback set inside StatefulBuilder builder (HIGH)
* `settings_screen.dart`: BLE started after showDialog without await — race (HIGH)
* `wake_calibration_screen.dart`: DB write after BLE — desync if DB fails; no mounted guard before context.read (HIGH)
* `wrist_flick_calibration_screen.dart`: DB written BEFORE BLE — inverted priority; full config write on every slider drag (HIGH)
* `settings_screen.dart`: Role swap with uninitialized repo silently sets role to 'A' (HIGH)
*(Plus 14 Medium/Low issues including context.read in dispose, hardcoded previews, and magic numbers)*

## 3. Flutter Services (31 Issues)
**Critical/High:**
* `ble_service.dart`: Write queue grows without bound / deadlock on hung write (CRITICAL)
* `ble_service.dart`: `_onConnected` is async void, exceptions silently swallowed (CRITICAL)
* `ble_service.dart`: `cancelCalibration` permanently kills calibration subscription (CRITICAL)
* `ble_service.dart`: `sendCalibCmd` skips connection state check (HIGH)
* `ble_service.dart`: `startCalibration`/`startCompassCalibration` unawaited call (HIGH)
* `ble_service.dart`: Double reconnect timer on BLE stack quirks (HIGH)
* `ble_service.dart`: Stale characteristic subscriptions leak on retry (HIGH)
* `ble_service.dart`: Unix epoch silently truncated to uint32 (Y2038 risk) (HIGH)
* `foreground_service.dart`: `start()` always returns `true` ignoring service result (HIGH)
* `foreground_service.dart`: `WidgetsFlutterBinding` called in background isolate (HIGH)
* `location_service.dart`: `_isRunning` not rechecked after async GPS call (HIGH)
* `location_service.dart`: `_doGpsUpdate()` unawaited in radar mode handler (HIGH)
* `location_service.dart`: Android 10+ `requestPermission` for "always" silently fails (HIGH)
*(Plus 19 Medium/Low issues)*

## 4. Flutter Core (26 Issues)
**Critical/High:**
* `main.dart`: Hardcoded JWT secret in source (CRITICAL)
* `main.dart`: Hardcoded private IP + plain HTTP (CRITICAL)
* `sync_service.dart`: `late _client` used before `initialize()` (CRITICAL)
* `main.dart`: Null-dereference on `_selectedUserRole!` in listener (HIGH)
* `main.dart`: Re-entrant `_bootstrap()` — no guard flag (HIGH)
* `main.dart`: No `mounted` checks after `await` in `_bootstrap()` (HIGH)
* `sync_service.dart`: Misleading indentation in try/finally (maintenance hazard) (HIGH)
* `sync_service.dart`: Haptic event can be processed twice (fire-and-forget consume) (HIGH)
* `sync_service.dart`: No Realtime reconnect logic (HIGH)
* `bearing_calculator.dart`: `bearingToLedIndex` wrong for negative/large bearings; `round()` boundary issues (HIGH)
* `bearing_calculator.dart`: Dead code: `_earthRadiusKm` never used (HIGH)
* `location_model.dart`: `isValid` rejects valid (0,0) Gulf of Guinea coordinates (HIGH)
*(Plus 14 Medium/Low issues)*

## 5. Firmware: Power, LED, Haptic, Config (18 Issues)
**Critical/High:**
* `power.cpp`: WAKE_UP_DUR bits shifted to wrong position (<<5 should be <<4) (HIGH)
* `haptic.cpp`: begin() calls digitalWrite before pinMode — floating MOSFET gate (HIGH)
* `power.cpp/config.h`: IMU ODR mismatch: config says 26Hz, register programs 12.5Hz (HIGH)
* `haptic.cpp`: stopMotor() early-return skips PWM zero + sets pattern_active wrong order (HIGH)
*(Plus 14 Medium/Low issues including busy-waits starving FreeRTOS and floating battery ADC pins)*

## 6. Firmware: Sensors, Gestures, Compass (27 Issues)
**Critical/High:**
* `imu_calibrator.cpp`: Wrong LSB scale: 31.25 used instead of 62.5 — 2× sensitivity error (CRITICAL)
* `gesture.cpp`: `sequence_start_ms` not in constructor initializer list (HIGH)
* `gesture.cpp`: Static `tear_debounce` survives reset() — cross-state leak (HIGH)
* `gesture.cpp`: Timeout logic resets flick_count outside guard — drops unreported gesture (HIGH)
* `compass.cpp`: `powerUp()` calls `i2c_stop()` with no prior `i2c_start()` — invalid bus state (HIGH)
* `runtime_config.h`: Struct padding makes binary layout fragile across refactors (HIGH)
* `runtime_config.cpp`: Migration path copies raw bytes without field validation (HIGH)
*(Plus 20 Medium/Low issues including moving average logic bugs and missing I2C timeout resets)*

## 7. Firmware: State Machine, BLE, Core (36 Issues)
**Critical/High:**
* `nexus_halo.ino`: `clearStateChanged()` before `update()` erases fresh flags (HIGH)
* `nexus_halo.ino`: `state_changed` never cleared after switch block (HIGH)
* `nexus_halo.ino` / `ble_handler.cpp`: `ble_connected` not volatile, written from ISR (HIGH)
* `ble_handler.cpp`: ISR-written BLE fields not volatile (HIGH)
* `ble_handler.cpp`: `config_json_buf` race in `notifyConfig()` (HIGH)
* `nexus_halo.ino`: WAKING_UP transitions unconditionally every tick (HIGH)
* `nexus_halo.ino`: `Serial.print()` in BLE ISR callback (HIGH)
* `nexus_halo.ino`: `STATE_COMPASS_CALIBRATION` has no timeout (HIGH)
* `ble_handler.cpp`: `Serial.print()` in BLE write callback (ISR context) (HIGH)
* `nexus_halo.ino` / `ble_handler.cpp`: `xSemaphoreGive` (non-ISR) called from potential ISR context (HIGH)
*(Plus 26 Medium/Low issues including duplicate battery timers and unhandled state cancellation paths)*

---
**Total New Issues Identified in Phase 2:** 158 Issues (7 Critical, 41 High, 69 Medium, 41 Low)
