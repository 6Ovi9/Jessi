diff --git a/README.md b/README.md
index f36e42e..fdb25b6 100644
--- a/README.md
+++ b/README.md
@@ -63,7 +63,7 @@ En la versi├│n v2.1.0, el reloj se despierta del modo `DEEP_SLEEP` ├║nicamente a
 ### ≡ƒ¢í∩╕Å Salvaguarda Contra Notificaciones Accidentales (Flujo en Dos Fases)
 El sistema ha sido dise├▒ado espec├¡ficamente para que no se puedan enviar notificaciones accidentales mientras el reloj est├í en un bolso o mochila o durante movimientos corporales bruscos:
 1.  **Fase 1: Despertar (Aceler├│metro).** Cuando el reloj est├í en reposo profundo (`DEEP_SLEEP`), el giroscopio est├í **totalmente apagado**. El aceler├│metro de ultra bajo consumo solo vigila el levantamiento de mu├▒eca. Un movimiento fuerte o levantamiento despierta el reloj al modo `CLOCK` para ver la hora.
-2.  **Fase 2: Detecci├│n de Gestos (Giroscopio).** El giroscopio solo se enciende *despu├⌐s* de que el reloj ha despertado. Una vez en modo `CLOCK`, el motor de gestos analiza la velocidad angular en busca de un **Doble Giro de Mu├▒eca** r├ípido (*Double Flick*) dentro de una ventana temporal ajustable (de 400 a 1200 ms). Si el movimiento que despert├│ al reloj no es seguido inmediatamente por este doble giro coordinado, el reloj vuelve a dormirse tras unos segundos sin enviar ning├║n toque.
+2.  **Fase 2: Detecci├│n de Gestos (Giroscopio).** El giroscopio solo se enciende *despu├⌐s* de que el reloj ha despertado. Una vez en modo `CLOCK`, el motor de gestos analiza la velocidad angular en busca de un **Triple Giro de Mu├▒eca** r├ípido (*Triple Flick*) dentro de una ventana temporal ajustable (de 400 a 1200 ms). Si el movimiento que despert├│ al reloj no es seguido inmediatamente por este triple giro coordinado, el reloj vuelve a dormirse tras unos segundos sin enviar ning├║n toque.
 
 ### ≡ƒ¢á∩╕Å Compilaci├│n e Instalaci├│n del Firmware
 
diff --git a/nexus_halo/compass.cpp b/nexus_halo/compass.cpp
index e5824dd..89f8bb1 100644
--- a/nexus_halo/compass.cpp
+++ b/nexus_halo/compass.cpp
@@ -480,7 +480,7 @@ void CompassController::calibrationUpdate() {
 }
 
 void CompassController::saveCalibration() {
-  struct CompassCalibData {
+  struct __attribute__((packed)) CompassCalibData {
     float offset_x;
     float offset_y;
     float offset_z;
@@ -532,7 +532,7 @@ void CompassController::saveCalibration() {
 }
 
 void CompassController::loadCalibration() {
-  struct CompassCalibData {
+  struct __attribute__((packed)) CompassCalibData {
     float offset_x;
     float offset_y;
     float offset_z;
diff --git a/nexus_halo/nexus_halo.ino b/nexus_halo/nexus_halo.ino
index 20c8fcf..13368bf 100644
--- a/nexus_halo/nexus_halo.ino
+++ b/nexus_halo/nexus_halo.ino
@@ -426,7 +426,7 @@ void loop() {
   uint32_t now_ms = millis();
 
   noInterrupts();
-  if (time_synced && (millis() - millis_at_sync > 86400000UL)) {
+  if (time_synced && (now_ms - millis_at_sync > 86400000UL)) {
     unix_base_ts += 86400; // Advance 1 day in seconds
     millis_at_sync += 86400000UL;
   }
@@ -849,6 +849,12 @@ void handleStateDeepSleep(GestureType gesture) {
     Serial.println("[POWER] Entering DEEP_SLEEP state. Configuring IMU for "
                    "Rise-to-Wake...");
 
+    if (config_save_pending) {
+      runtime_config.saveToFlash();
+      config_save_pending = false;
+      Serial.println("[CONFIG] Deferred config saved before sleep");
+    }
+
     // Power off peripherals on entry
     led_controller.setPower(false); // idempotent after BUG-010 fix in setPower()
     haptic.stopMotor();             // idempotent after BUG-010 fix in stopMotor()
@@ -892,6 +898,13 @@ void handleStateDeepSleep(GestureType gesture) {
     return;
   }
 
+  // Periodic Low Battery Pulse in Deep Sleep
+  static uint32_t last_low_batt_pulse_ms = 0;
+  if (power_manager.isLowBattery() && (millis() - last_low_batt_pulse_ms >= TIMER_LOW_BATTERY_PULSE_MS)) {
+    last_low_batt_pulse_ms = millis();
+    led_controller.errorBattery();
+  }
+
   // Put CPU to sleep until next interrupt (unless disabled for testing)
   #if !DEBUG_DISABLE_DEEP_SLEEP
     power_manager.enterDeepSleep();
@@ -970,6 +983,11 @@ void handleStateClockConnected(GestureType gesture, uint32_t now_ms) {
     local_tz = tz_offset_s;
     interrupts();
 
+    if (!time_synced && local_tz < 0 && (current_unix_ms / 1000ULL) < (uint32_t)abs(local_tz)) {
+        led_controller.updateClockTime(0, 0, 0, 0);
+        led_controller.showClock(true); // connected = white colors
+        return;
+    }
     uint32_t total_secs = (uint32_t)(current_unix_ms / 1000ULL) + local_tz;
     
     uint8_t seconds = total_secs % 60;
@@ -1032,6 +1050,11 @@ void handleStateClockDisconnected(GestureType gesture, uint32_t now_ms) {
     local_tz = tz_offset_s;
     interrupts();
 
+    if (!time_synced && local_tz < 0 && (current_unix_ms / 1000ULL) < (uint32_t)abs(local_tz)) {
+        led_controller.updateClockTime(0, 0, 0, 0);
+        led_controller.showClock(false); // disconnected = blue colors
+        return;
+    }
     uint32_t total_secs = (uint32_t)(current_unix_ms / 1000ULL) + local_tz;
     
     uint8_t seconds = total_secs % 60;
@@ -1183,19 +1206,21 @@ void handleStateHapticTX() {
     ble_handler.notifyHapticTX();
     if (runtime_config.getConfig().hapticPatternIndex ==
         1) {                                      // 1 = only partner
-      haptic.playPattern(HAPTIC_PATTERN_BATTERY); // confirm with a short pulse
-      state_machine.resetTimer(haptic.getPatternLength(HAPTIC_PATTERN_BATTERY) + 500);
+      // Do nothing locally
     } else {
-      haptic.playPattern(HAPTIC_PATTERN_RX); // long vibration pattern
-      state_machine.resetTimer(haptic.getPatternLength(HAPTIC_PATTERN_RX) + 500);
+      // Small pulse
+      uint8_t brightness_pct = runtime_config.getConfig().brightnessPercent;
+      uint8_t base_brightness = (brightness_pct * 255) / 100;
+      led_controller.fillWithBrightness(COLOR_HAPTIC_RX, base_brightness);
+      delay(100);
+      led_controller.clear();
+      led_controller.show();
+      delay(100);
+      led_controller.fillWithBrightness(COLOR_HAPTIC_RX, base_brightness);
+      delay(100);
+      led_controller.clear();
+      led_controller.show();
     }
-
-    uint8_t brightness_pct = runtime_config.getConfig().brightnessPercent;
-
-    uint8_t base_brightness = (brightness_pct * 255) / 100;
-
-    // Usar rosa (COLOR_HAPTIC_RX) escalado por brillo seguro
-    led_controller.fillWithBrightness(COLOR_HAPTIC_RX, base_brightness);
     pattern_started = true;
   }
 }
diff --git a/nexus_halo/power.cpp b/nexus_halo/power.cpp
index 7ce092b..3847306 100644
--- a/nexus_halo/power.cpp
+++ b/nexus_halo/power.cpp
@@ -144,7 +144,7 @@ void PowerManager::setupIMUForRiseToWake() {
   // 5. Set wake-up duration (bits[7:6] of WAKE_UP_DUR).
   //    Each count = 1/ODR_XL = 80 ms at 12.5 Hz.
   //    Also clear SLEEP_DUR and TIMER_HR bits ΓåÆ write full register = 0xXX.
-  _writeLSM6DS3Register(LSM6DS3_REG_WAKE_UP_DUR, (IMU_WAKE_UP_DUR & 0x03) << 6);
+  _writeLSM6DS3Register(LSM6DS3_REG_WAKE_UP_DUR, (IMU_WAKE_UP_DUR & 0x03) << 5);
   Serial.print("[POWER] Wake duration: ");
   Serial.print(IMU_WAKE_UP_DUR + 1);
   Serial.print(" sample(s) = ~");
